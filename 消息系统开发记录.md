# 消息系统开发记录

开发日期：2026-06-11

## 一、功能目标

管理后台发布公告后，用户在消息中心收到提醒。支持公告通知和个人通知两种类型，已读状态跨设备同步。

## 二、后端实现（server.js）

### 新增 API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/notifications | 获取当前用户的通知（公告 + 个人通知） |
| PUT | /api/notifications/:id/read | 标记单条通知已读 |
| POST | /api/notifications/batch-read | 批量标记已读 |
| POST | /api/notifications/batch-delete | 批量删除个人通知（不能删除公告） |
| POST | /api/notifications | 创建个人通知（系统内部调用） |

### 实现逻辑

1. 动态将已发布的公告转换为通知（不存储副本）
2. 合并用户的个人通知
3. 使用 `notification_reads` 数组记录已读状态（支持多设备同步）
4. 按时间倒序排序（置顶公告排最前）

### 数据结构

```json
{
  "notifications": [],
  "notification_reads": []
}
```

`notification_reads` 条目格式：`{ userId, noticeId, readAt }`

### 设计决策

**为什么动态转换公告而不是存储副本？**
节省存储空间，公告更新后用户立即看到最新版本，不需要同步更新所有用户的通知。

**为什么使用 notification_reads 记录已读状态？**
已读状态存储在服务端，切换设备后仍然有效；可以为每个用户独立记录；未来可扩展为"全部已读"、"按公告已读"等功能。

## 三、前端修改（messages.html）

- 添加 `getAuthToken()` 和 `authFetch()` 辅助函数
- 所有 API 调用添加 Authorization header：`Bearer <token>`
- Token 来源：`localStorage.getItem('token') || sessionStorage.getItem('token')`

## 四、API 详细文档

### GET /api/notifications

获取当前用户的通知列表。

**Headers:** `Authorization: Bearer <token>`

**Response:**
```json
{
  "success": true,
  "data": [
    {
      "id": "notice_1234567890",
      "originalId": 1234567890,
      "title": "平台公告",
      "content": "<p>公告内容...</p>",
      "type": "announcement",
      "isHtml": true,
      "read": false,
      "readAt": null,
      "createdAt": "2026-06-11T10:00:00.000Z",
      "pinned": true
    }
  ]
}
```

### PUT /api/notifications/:id/read

标记单条通知已读。返回 `{ "success": true }`

### POST /api/notifications/batch-read

**Body:** `{ "ids": ["notice_123", "notification_456"] }`

**Response:** `{ "success": true, "count": 2 }`

### POST /api/notifications/batch-delete

批量删除个人通知（不能删除公告）。

**Body:** `{ "ids": ["notification_456", "notification_789"] }`

**Response:** `{ "success": true, "count": 2 }`

## 五、Bug 修复记录

### 去掉消息列表类型徽章的多余样式

**问题**: 消息列表中的类型徽章（如 "平台公告"）带有圆角背景框，与设计不符。

**修复**: `.type-badge` 基础样式中去掉 `border-radius`、`padding` 设为 0、`background` 设为 `transparent`，改为纯文字展示。

## 六、修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| server.js | 修改 | 添加通知相关 API（第 1526-1720 行附近） |
| messages.html | 修改 | 添加认证 header、修复 API 调用 |
| js/notification.js | 修改 | 修复 API 返回格式匹配、markAllNotificationsRead 逻辑 |
| training-plan.html | 修改 | 补充 notification.js 引用 |

## 七、待办事项

- [ ] 通知按类型筛选（公告、系统、培训等）
- [ ] 通知搜索
- [ ] 分页加载（当前一次加载所有）
